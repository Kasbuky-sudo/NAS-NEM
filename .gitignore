# 依赖与本机数据
node_modules/
data/
.workbuddy/
.cache/

# 构建产物
packaging/fnOS/.build-staging/
packaging/fnOS/dist/

# 本机诊断脚本（含内网地址与凭据，绝不入库）
packaging/fnOS/scripts/nas-*.py
scripts/

# 运行日志
*.log

# skills/ 已入库（脱敏模板：<NAS_IP> 占位，无凭据）。
# 各人自用的带真实 IP/凭据副本放 ~/.workbuddy/skills/，勿提交回这里。
